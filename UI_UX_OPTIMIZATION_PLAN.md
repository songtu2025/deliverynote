# DeliveryNote PC 界面规范

| 文档属性 | 说明 |
| --- | --- |
| 文档状态 | 已实施基线；后续界面变更的验收规范 |
| 适用产品 | DeliveryNote Web |
| 适用终端 | PC 浏览器 |
| 不适用范围 | 手机端专项布局、营销页面、通用设计系统建设 |
| 主要读者 | 产品负责人、前端维护人员、测试与发布复核人员 |
| 最近整理 | 2026-07-23 |

文件名为兼容已有链接继续保留。本文不是开发待办清单，而是当前界面的受控规范；实际验收记录见 [PC 界面验收报告](design-qa.md)。

## 1. 设计目标

界面服务于不超过 5 名内部用户，优先保证：

1. 操作人员能够判断“当前处于哪一步、接下来做什么”；
2. 采购、交货、可导入和待处理数量可直接核对；
3. 文件顺序、锁定资料和规则版本可追溯；
4. 危险或不可逆操作有明确说明、校验和确认；
5. 常见 PC 视口内主操作、表格操作和弹窗底部按钮可达；
6. 界面变化不改变业务规则、数据含义或导出契约。

### 1.1 需求级别

| 级别 | 含义 |
| --- | --- |
| `P0` | 业务安全或数据一致性要求；不满足时不得发布相关功能 |
| `P1` | 核心操作效率和可理解性要求；发布前应满足 |
| `P2` | 体验增强；可以记录限制，但不得影响 P0/P1 |

## 2. 信息架构与权限

### 2.1 一级导航

| 区域 | 页面 | `operator` | `admin` |
| --- | --- | :---: | :---: |
| 批次处理 | 批次列表、新建批次、批次详情、异常审校、导出 | ✓ | ✓ |
| 超收规则 | 当前规则、发布新版本、历史版本 | ✓ | ✓ |
| 管理员维护 | 基础资料、库位草稿、用户账号、操作记录 | — | ✓ |

### 2.2 导航需求

| 编号 | 级别 | 要求 |
| --- | :---: | --- |
| `UI-NAV-001` | P1 | 一级导航名称使用业务语言，当前区域有稳定的选中状态。 |
| `UI-NAV-002` | P0 | 用户看不到无权限入口；直接访问无权限页面时由服务端权限继续兜底。 |
| `UI-NAV-003` | P1 | 批次详情使用 `/batches/{id}` 独立 URL，刷新保持当前批次，浏览器返回回到上一个历史页面。 |
| `UI-NAV-004` | P1 | 页面切换后视口回到顶部，不继承上一页面的滚动位置。 |

## 3. 全局界面框架

| 编号 | 级别 | 要求 |
| --- | :---: | --- |
| `UI-SHELL-001` | P1 | 常规页面使用统一侧栏、标题层级和内容宽度；批次详情可隐藏侧栏形成全宽工作台。 |
| `UI-SHELL-002` | P1 | 右上角账号区显示头像首字母、用户名和中文角色。 |
| `UI-SHELL-003` | P0 | “退出登录”是独立、可读的操作；退出调用服务端登出、清除本地会话并返回登录页。 |
| `UI-SHELL-004` | P1 | 系统时间统一显示为北京时间，不随浏览器本地时区变化。 |
| `UI-SHELL-005` | P1 | 页面级加载、空数据、无筛选结果、失败和权限不足使用不同且明确的状态说明。 |

全局不增加与当前业务无关的个人资料、消息中心、主题切换或折叠菜单。

## 4. 批次列表

| 编号 | 级别 | 要求 |
| --- | :---: | --- |
| `UI-LIST-001` | P1 | 页面顶部显示五类基础资料是否就绪，并提供明确的新建批次入口。 |
| `UI-LIST-002` | P1 | 搜索与状态筛选具有始终可见的标签，不仅依赖占位文字。 |
| `UI-LIST-003` | P1 | 表格按批次名称、状态、文件数、交货数量、下一步和更新时间形成稳定阅读顺序。 |
| `UI-LIST-004` | P1 | 批次名称或明确按钮可进入详情；整行不使用含义不明的隐藏点击行为。 |
| `UI-LIST-005` | P1 | 新建批次弹窗可以安全取消；提交期间防止重复创建。 |

批次列表按少量内部数据设计。只有真实数据规模使扫描或加载明显变慢时，才引入服务端分页。

## 5. 批次详情工作台

批次主流程固定为：

```text
准备文件 → 预检 → 计算结果 → 异常审校 → 导出下载
```

### 5.1 页面骨架

| 编号 | 级别 | 要求 |
| --- | :---: | --- |
| `UI-BATCH-001` | P1 | 顶部依次呈现批次身份、五步流程、当前阶段说明、数量摘要、来源文件和锁定资料。 |
| `UI-BATCH-002` | P0 | 数量摘要同时显示交货总量、可导入总量、待处理总量，并能核对守恒关系。 |
| `UI-BATCH-003` | P0 | 来源文件顺序清晰可见；计算前可调整，计算开始后不可伪装为仍可调整。 |
| `UI-BATCH-004` | P1 | 页面刷新后从服务端恢复批次、任务和结果状态，不退回批次列表。 |
| `UI-BATCH-005` | P1 | 当前阶段只突出一个主要下一步；辅助操作保持次级层级。 |

### 5.2 准备与预检

| 编号 | 级别 | 要求 |
| --- | :---: | --- |
| `UI-BATCH-006` | P0 | 显示本批次锁定的五类基础资料和超收规则版本。 |
| `UI-BATCH-007` | P1 | 上传后显示文件名和顺序；计算前允许删除错传文件。 |
| `UI-BATCH-008` | P0 | 预检失败必须指明文件和可理解的失败原因；未通过预检不得启动计算。 |
| `UI-BATCH-009` | P0 | 上传、删除、排序、预检和启动计算在请求期间禁止重复提交。 |

### 5.3 计算与异常审校

| 编号 | 级别 | 要求 |
| --- | :---: | --- |
| `UI-BATCH-010` | P1 | 计算状态区分排队、运行、成功和失败；失败时显示可操作的错误信息。 |
| `UI-BATCH-011` | P1 | 存在待处理数量时，“异常审校”是当前主操作。 |
| `UI-BATCH-012` | P1 | 待处理区支持搜索，以及站点、规模定位、备货定位、原因和处理状态筛选；筛选器有可见标签。 |
| `UI-BATCH-013` | P0 | 拆分抽屉显示来源、SKU、站点、仓库、原数量和拆分后守恒结果。 |
| `UI-BATCH-014` | P0 | 拆分数量非法或总和不等时禁止保存；保存期间不得关闭抽屉或重复提交。 |

### 5.4 导出

| 编号 | 级别 | 要求 |
| --- | :---: | --- |
| `UI-EXPORT-001` | P1 | 导出入口集中在同一结果操作区，不在页面底部重复放置含义相同的卡片。 |
| `UI-EXPORT-002` | P0 | 单文件批次提供对应结果下载；多文件批次同时提供合并 Excel 和分文件 ZIP。 |
| `UI-EXPORT-003` | P1 | 合并 Excel 与分文件 ZIP 的按钮文案明确说明结果形态。 |
| `UI-EXPORT-004` | P0 | 待处理未清零时允许按现有业务规则导出，但必须显示剩余待处理数量提示。 |
| `UI-EXPORT-005` | P1 | 下载期间防止重复触发；失败时保留当前页面并显示错误原因。 |

“来源文件与处理顺序”中的来源下载用于获取该来源对应的处理结果，不替代批次级合并 Excel 或 ZIP。

## 6. 基础资料

### 6.1 资料目录

| 编号 | 级别 | 要求 |
| --- | :---: | --- |
| `UI-DATA-001` | P1 | 采购、商品、供应商、库位、模板五类资料使用同一横向目录，避免分散入口。 |
| `UI-DATA-002` | P1 | 当前类型同时显示状态、版本、文件、创建人、更新时间和可用操作。 |
| `UI-DATA-003` | P1 | 资料用途、必填字段和业务影响可展开查看，不与高频操作争夺首屏。 |
| `UI-DATA-004` | P1 | 数据预览、版本记录和质量检查使用明确标签页，切换不改变当前资料类型。 |
| `UI-DATA-005` | P0 | 库位已有正式版本后，不提供绕过服务器草稿的普通替换发布入口。 |

### 6.2 库位草稿工作台

| 编号 | 级别 | 要求 |
| --- | :---: | --- |
| `UI-DRAFT-001` | P1 | 顶部持续提供返回、下载、Excel 替换、校验、发布和放弃草稿。 |
| `UI-DRAFT-002` | P0 | 自动保存状态、草稿修订号、正式基线版本和最后更新时间可见。 |
| `UI-DRAFT-003` | P1 | 摘要显示记录数、变更数、错误数、警告数和相对正式版差异。 |
| `UI-DRAFT-004` | P1 | 支持搜索、精确筛选、问题筛选、仅看修改和批量删除。 |
| `UI-DRAFT-005` | P1 | 新增/编辑使用适合 PC 的两列表单；字段有可见标签和用途说明。 |
| `UI-DRAFT-006` | P0 | Excel 整表替换在应用前展示增、改、删、错误和警告。 |
| `UI-DRAFT-007` | P0 | 存在错误时发布按钮禁用；存在警告时必须显式确认。 |
| `UI-DRAFT-008` | P0 | 发布事务再次校验草稿基线；正式版本变化时阻止旧草稿覆盖新版本。 |
| `UI-DRAFT-009` | P1 | 发布弹窗内容可滚动，标题与底部操作在目标 PC 高度内保持可达。 |

## 7. 超收规则

| 编号 | 级别 | 要求 |
| --- | :---: | --- |
| `UI-RULE-001` | P1 | 当前启用规则、发布新版本和历史版本三个区域层次清楚。 |
| `UI-RULE-002` | P0 | 发布界面明确说明版本不可修改，发布只影响之后创建的批次。 |
| `UI-RULE-003` | P1 | 短尾、中尾、长尾额度输入显示“件”，并校验为允许的非负整数。 |
| `UI-RULE-004` | P0 | 仓库为空时明确显示“未开放任何仓库”，不得暗示全部允许。 |
| `UI-RULE-005` | P1 | 历史表显示版本、三类额度、仓库、发布时间和发布人。 |
| `UI-RULE-006` | P0 | 重新启用操作明确指出目标版本，并遵守不可变版本语义。 |

## 8. 用户与操作记录

| 编号 | 级别 | 要求 |
| --- | :---: | --- |
| `UI-ADMIN-001` | P1 | 用户页显示总数、管理员、操作员和停用账号摘要。 |
| `UI-ADMIN-002` | P0 | 当前登录账号不可停用；停用或密码重置操作有明确确认与结果反馈。 |
| `UI-ADMIN-003` | P1 | 用户表显示账号、角色、状态、创建时间和适用操作。 |
| `UI-AUDIT-001` | P1 | 操作记录按时间倒序显示时间、操作人、操作和对象。 |
| `UI-AUDIT-002` | P1 | 提供搜索、操作类型和操作人筛选，并明确范围为最近 200 条。 |
| `UI-AUDIT-003` | P0 | 库位草稿的创建、修改、Excel 替换、校验、发布和放弃均应形成可理解的审计事件。 |

## 9. 视觉语言

### 9.1 基础标记

| 项目 | 规范 |
| --- | --- |
| 品牌主色 | `#176b5b` |
| 页面背景 | 浅中性灰 |
| 内容表面 | 白色 |
| 字体 | `Inter, Microsoft YaHei, sans-serif` |
| 图标 | 优先使用现有 Ant Design 图标 |
| 常规区块间距 | 约 16–18 px，按信息层级保持一致 |

### 9.2 使用原则

- 状态颜色只表达成功、就绪、警告、失败和待处理；
- 状态不能只依赖颜色，必须同时有文本或图标语义；
- 标题、说明、表格和辅助文字形成稳定层级；
- 边框、圆角和阴影保持克制；
- 不使用 emoji、临时占位图或装饰动画作为正式业务控件；
- 危险操作使用明确动词，不使用仅图标或含糊的“确定”。

## 10. 状态与反馈

| 场景 | 界面要求 |
| --- | --- |
| 加载中 | 保留页面结构，说明正在加载的对象 |
| 请求成功 | 反馈实际完成的动作，并刷新服务端状态 |
| 请求失败 | 显示原操作和可理解原因，不伪装为成功 |
| 无数据 | 说明当前尚无数据，并在有权限时提供下一步 |
| 无筛选结果 | 保留筛选条件，允许一键清除或逐项调整 |
| 并发冲突 | 说明数据已变化，要求刷新或重新建立草稿 |
| 登录失效 | 清理本地会话并返回登录页 |
| 长任务 | 显示排队/运行状态，允许刷新后恢复 |

计算、发布、删除、放弃草稿、重置密码和拆分等重要操作在请求期间必须禁用重复提交。

## 11. 可访问性基线

| 编号 | 级别 | 要求 |
| --- | :---: | --- |
| `UI-A11Y-001` | P1 | 搜索框、筛选器、表格和图标按钮具有可读名称。 |
| `UI-A11Y-002` | P1 | 所有表单字段使用可见标签，错误与对应字段关联。 |
| `UI-A11Y-003` | P1 | 键盘焦点清晰可见，不被全局样式移除。 |
| `UI-A11Y-004` | P1 | 状态不只依赖颜色表达。 |
| `UI-A11Y-005` | P1 | 弹窗和抽屉打开后焦点进入其内容，关闭后返回触发控件。 |

当前基线不等同于完整 WCAG 合规认证。屏幕阅读器全流程、完整键盘遍历和自动对比度测量仍需在出现正式合规要求时单独执行。

## 12. PC 视口与布局

正式验收视口：

```text
1280 × 800
1440 × 900
1920 × 1080
```

| 编号 | 级别 | 要求 |
| --- | :---: | --- |
| `UI-VIEW-001` | P1 | 页面不得出现非业务需要的整体横向滚动。 |
| `UI-VIEW-002` | P1 | 标题、当前状态和主要操作位于合理首屏范围。 |
| `UI-VIEW-003` | P1 | 表格操作列在目标视口内可访问。 |
| `UI-VIEW-004` | P1 | 抽屉与弹窗底部操作无需改变浏览器缩放即可到达。 |

不以手机端布局作为发布验收条件，也不为手机端增加独立导航和表格卡片化逻辑。

## 13. 变更与验收流程

界面变更应按以下顺序执行：

1. 标明受影响的需求编号；
2. 阅读对应页面测试和 API 调用链；
3. 以最小范围修改界面；
4. 更新或新增自动化测试；
5. 运行前端完整测试与生产构建；
6. 在 1280×800 和 1440×900 进行真实浏览器复核；
7. 涉及宽屏布局时补查 1920×1080；
8. 涉及接口、业务状态、数量或导出时运行 Python 完整测试；
9. 在 [验收报告](design-qa.md) 中记录证据、未覆盖项和残余风险。

最低命令：

```bash
cd frontend
npm run test
npm run build
```

涉及后端或业务状态：

```bash
source .venv/bin/activate
python -m unittest discover -s tests -v
python -m pip check
```

路由、下载、焦点、弹窗高度和北京时间必须在真实浏览器中复核，不能只依赖组件测试。

## 14. 变更控制原则

- 真实操作反馈优先于无业务依据的大范围视觉重构；
- 不通过 UI 调整改变采购、超收、仓库、拆分或导出的业务含义；
- 不为消除构建体积提示优先引入复杂拆包；
- 不为当前少量用户建设主题平台、组件平台或移动端；
- P0 需求变化必须同步更新 README 业务契约、测试和验收报告；
- 新页面必须复用本规范的导航、状态、权限、时间和可访问性规则。
