# DeliveryNote 操作流程与 UI 优化方案

- 方案版本：1.0
- 更新时间：2026-07-21
- 采用方向：方案 1「批次工作台」
- 当前状态：核心功能、Linux 部署和管理员维护 PC 浏览器视觉验收已完成；生产可用仍受完整业务验收和备份恢复门槛约束

## 1. 优化目标

本次优化不改变采购匹配、仓库优先级、锁仓规则、Excel 字段或导出命名，只改善操作路径、信息呈现和错误预防。

目标如下：

1. 把批次处理收敛成可理解、可恢复的五步流程。
2. 在任何关键操作前明确当前状态、下一步和阻塞原因。
3. 让文件顺序与采购余额扣减关系始终可见，降低误操作风险。
4. 让异常审校围绕“数量守恒”完成，保存前即时阻止无效拆分。
5. 让管理员一眼看出五类基础资料是否齐备。
6. 保持后台任务可恢复，用户离开页面再返回仍能看到计算或导出进度。

## 2. 必须保持的业务边界

- 同一批次的交货文件按用户指定顺序共享并连续扣减采购余额。
- 所有交货数量必须保留，超量、未匹配和歧义部分进入待处理。
- 供应商成品本地仓优先，其他仓库保持确定性顺序。
- 商品锁仓标识继续解决 SKU/站点歧义。
- 拆分数量必须全部为正数，拆分总和必须等于原待处理数量。
- 始终满足 `交货总量 = 可导入总量 + 待处理总量`。
- 保持 CLI、模板 A:G 字段、样式、交货备注和导出命名兼容。
- 不跨批次延续采购扣减，不做 ERP 回写。

## 3. 信息架构

### 操作员

1. 批次列表：查看所有批次、基础资料就绪状态和下一步动作。
2. 批次工作台：完成准备文件、预检、计算、异常审校和导出。

### 管理员

1. 基础资料：维护采购、商品、供应商、库位和模板版本。
2. 用户账号：创建用户、启停账号、重置密码。
3. 操作记录：查看最近的用户与 Worker 操作。

## 4. 核心操作流程

### 前置条件：五类基础资料就绪

- 批次列表顶部显示采购、商品、供应商、库位和模板的当前启用版本。
- 缺少任一启用版本时禁用“新建批次”，并直接提示管理员需要补齐的资料。
- 新批次创建时锁定当时的五类版本；以后启用新版本不影响已有批次。
- 上传输入资料后由服务端立即校验 Excel，错误文件不入库、不启用。

### 第 1 步：准备文件

- 支持一次选择多个交货 Excel。
- 文件表固定显示处理序号、来源文件、供应商、交货/可导入/待处理数量和操作。
- 页面持续显示“序号越小，越先扣减采购余额”。
- 允许上移、下移和删除错传文件；顺序变化或删除后自动要求重新预检。
- 计算开始后锁定文件和顺序，避免结果与界面状态不一致。

### 第 2 步：预检

- 预检交货文件格式、供应商识别、五类输入版本和模板结构。
- 预检失败在批次顶部显示明确错误，不进入后台计算。
- 预检通过后，主按钮切换为“启动计算”。

### 第 3 步：计算结果

- 计算通过后台 Worker 执行，页面显示任务状态。
- 用户可以离开页面；返回后从批次详情恢复任务并继续轮询。
- 计算完成后展示总量条：交货总量、可导入、待处理和数量守恒公式。
- 未计算前数量显示为“—”，避免把 0 误解成真实计算结果。
- 每个来源文件展示独立结果，便于核对顺序扣减的影响。

### 第 4 步：异常审校

- 异常表支持按来源文件/SKU/站点/目的仓搜索，按原因和状态过滤。
- 状态统一为“未处理、部分处理、已处理”。
- 点击“拆分审校”从右侧打开抽屉，保持原列表上下文。
- 抽屉实时显示原待处理量、已拆分量和剩余量。
- 数量不为正或合计不等于原数量时禁用保存。
- 标记为“可正式导入”的部分必须填写目的仓和完整站点。
- 修改拆分后使旧导出失效，明确提示重新生成。

### 第 5 步：导出下载

- 说明每个来源文件都会生成独立结果，并提供批次 ZIP。
- 明确“交货导入”和“待处理导入”两个工作表的数量关系。
- 即使仍有待处理数量也允许导出，但使用警告提示风险。
- 后台导出状态可恢复；完成后提供单文件下载和批次 ZIP 下载。

## 5. 页面方案

### 批次列表

- 顶部：页面标题、五类资料就绪条、新建批次按钮。
- 工具栏：关键词搜索、状态筛选。
- 表格：批次名称、状态、文件数、数量概览、更新时间、下一步。
- 新建弹窗：默认使用日期生成批次名，并展示将锁定的输入版本。

### 批次工作台

- 标题区：返回、批次名、业务状态、更新时间和当前可执行主操作。
- 流程区：横向五步状态条。
- 汇总区：四块数量摘要，突出守恒公式。
- 内容区：来源文件与顺序、锁定版本、异常审校、导出下载。
- 异常详情：右侧拆分抽屉。

### 管理员维护

- 基础资料：左侧五类资料目录说明用途和维护方式，右侧集中显示当前版本、数据摘要、质量提示、内容预览、下载、替换上传和版本历史。
- 库位维护工作区：从当前启用版本创建或继续服务器唯一草稿，支持搜索/筛选、逐行新增/修改/删除、批量删除、Excel 差异预览、发布校验和警告确认。
- 用户账号：创建、启停、重置密码；禁止管理员停用自己。
- 操作记录：显示操作时间、操作人、动作和对象。

## 6. 视觉与交互规范

- 延续 Ant Design 组件体系，不新建另一套组件库。
- 主色使用深绿色，背景使用浅灰绿，白色内容面承载业务信息。
- 主要状态依赖“文字 + 颜色”，不只依赖颜色。
- 主按钮只承载当前步骤最重要的动作；危险动作使用确认框。
- 数量、顺序和状态使用固定列宽，长文件名使用省略和提示。
- 本轮管理员维护仅面向 1280–1920px PC 端，保持高信息密度；不以平板或移动端适配作为完成门槛。
- 所有输入使用可见标签，图标按钮提供可访问名称或提示。

## 7. 后端/API 配套

- 批次详情返回文件数、数量汇总、锁定版本、来源文件和计算/导出任务。
- 来源文件数量在异常拆分后按最新投影重新汇总。
- 增加计算前删除交货文件接口，并自动重排其余文件。
- 输入版本上传后立即执行对应 Excel 校验，失败则删除文件并返回 400。
- 增加用户启停和管理员重置密码接口；停用或重置后清除目标用户会话。
- 任务接口返回创建、领取、心跳和完成时间，支持刷新后恢复进度。
- API 返回 401 时前端统一清理会话并回到登录页。

## 8. 已实施内容

- [x] 五步批次工作台与业务状态文案。
- [x] 基础资料就绪门槛和批次版本锁定展示。
- [x] 多文件上传、顺序提示、上移/下移和计算前删除。
- [x] 计算任务刷新恢复和总量守恒摘要。
- [x] 异常搜索/筛选、右侧拆分抽屉和实时守恒校验。
- [x] 导出说明、旧导出失效提示、单文件与 ZIP 入口。
- [x] 输入资料校验、版本历史、用户启停、密码重置和操作记录。
- [x] 五类资料目录、用途说明、当前版本摘要、预览和原文件下载。
- [x] 库位服务端持久化草稿、行级维护、筛选、Excel 差异确认、校验和原子发布新版本。
- [x] 管理员维护按“基础资料、用户账号、操作记录”重组为 PC 工作区。
- [x] 登录过期统一处理。
- [x] API、Worker、CLI 和前端交互回归测试。
- [x] Python 102/102、前端 54/54、生产构建和 Compose 重建验收。
- [x] 使用实际 Google Chrome 完成 1440×900 和 1920×1080 PC 视觉验收并保存脱敏证据。

## 9. 验收标准

### 自动化验收

- Python 全量单元/集成测试通过。
- `pip check` 无依赖冲突。
- 前端 Vitest 全部通过。
- 前端生产构建成功。
- `docker compose config` 成功。

### 业务验收

- 两个交货文件按 A、B 顺序计算得到 `160 = 100 + 60`。
- 调换顺序只改变余额归属，不改变交货总量。
- 无效拆分无法保存，有效拆分保存后仍数量守恒。
- 计算或导出期间刷新页面，任务状态能够恢复。
- 导出保持模板 A:G、样式、备注和命名兼容。

### 视觉与可访问性验收

- 参考图与实现使用相同桌面视口对照，不存在明显布局、间距或层级偏差。
- 1440×900 主验收视口和 1920×1080 扩展视口无重叠、裁切、页面级横向溢出或不可操作控件。
- 键盘可以到达核心操作，表单有标签，主要状态具有足够对比度。
- 加载、空、失败、禁用、成功和登录过期状态均有明确反馈。

## 10. 本轮不做

- 不引入 Redis、Celery 或微服务拆分。
- 不修改采购匹配、仓库优先级或 Excel 正式字段。
- 不做 ERP 回写或跨批次余额延续。
- 不在缺少实测依据时扩展多 Worker。
- 不把界面优化扩大成无关的品牌重做。

## 11. 终端查看与运行

查看本方案：

```bash
less UI_UX_OPTIMIZATION_PLAN.md
```

本地运行前端：

```bash
cd frontend
npm run dev
```

默认地址为 `http://127.0.0.1:5173`，前端会把 `/api` 和 `/health` 代理到 `127.0.0.1:8000`。
