# DeliveryNote PC 界面现状与维护规范

- 状态：当前版本已实施
- 适用范围：PC 浏览器
- 不适用范围：手机端专项设计
- 最近整理：2026-07-23

文件名为兼容已有链接继续保留。本文不再作为“待实施计划”，而是记录当前界面结构和后续修改边界。

## 1. 设计目标

界面服务于少量内部操作人员，优先级依次为：

1. 操作顺序清楚。
2. 数量和业务状态可核对。
3. 危险或不可逆操作有明确确认。
4. 重要入口在常见 PC 视口内可见。
5. 保持现有业务含义，不为视觉变化修改业务规则。

不追求营销型视觉、复杂动画、移动端重排或独立设计系统平台。

## 2. 信息架构

### 批次处理

- 批次列表
- 新建批次
- 批次详情工作台
- 待处理拆分
- 导出下载

### 超收规则

- 当前启用规则
- 发布不可变新版本
- 历史版本和重新启用

### 管理员维护

- 基础资料
- 用户账号
- 操作记录
- 库位/排仓网页维护

operator 可以处理批次和维护超收规则；管理员另有基础资料、账号和操作记录入口。

## 3. 批次操作流程

```text
准备文件 → 预检 → 计算结果 → 异常审校 → 导出下载
```

### 准备文件

- 展示本批次锁定的五类基础资料和超收规则。
- 上传一份或多份交货文件。
- 允许在计算前删除错传文件。
- 文件顺序直接决定采购余额和超收额度的消耗顺序。

### 预检

- 检查 Excel 结构、供应商识别和基础字段。
- 预检失败时明确指出文件和原因。
- 未通过预检不得启动计算。

### 计算结果

- 展示交货总量、可导入量和待处理量。
- 数量方程必须可见且守恒。
- Worker 任务状态支持刷新后恢复。

### 异常审校

- 当前存在待处理数量时，异常审校是主操作。
- 支持搜索、站点、规模定位、备货定位、原因和状态筛选。
- 拆分抽屉同时显示来源、SKU、站点、仓库、数量和守恒结果。
- 拆分保存期间不得关闭抽屉或离开页面。

### 导出下载

- 单文件批次下载对应处理结果。
- 多文件批次提供按来源顺序拼接的合并 Excel。
- 分文件 ZIP 只包含每个来源的独立工作簿。
- 已有单文件下载入口继续保留。
- 待处理未清零时仍可导出，但提示待处理数量。

## 4. 当前页面布局

### 批次列表

- 顶部显示基础资料就绪状态和新建批次入口。
- 搜索与状态筛选使用可见标签。
- PC 使用表格，批次名称是明确的打开入口。
- 状态、文件数、交货量、下一步和更新时间保持同一阅读顺序。

### 批次详情

- 使用 `/batches/{id}` 独立 URL。
- 刷新保持当前批次，浏览器返回回到批次列表。
- 详情页隐藏全局侧栏，为工作台保留完整横向空间。
- 顶部依次为批次信息、五步流程、当前阶段提示、数量摘要、来源文件和锁定资料。
- 待处理表格位于主流程中，不再额外放置重复导出卡片。

### 基础资料

- 五类资料使用同一横向目录。
- 当前类型显示版本、文件、创建人、时间和可用操作。
- 资料用途和业务影响可展开查看。
- 数据预览、版本记录和质量检查使用标签页。
- 库位已有正式版本后，只能进入服务器草稿维护流程。

### 库位维护

- 顶部固定显示返回、下载、Excel 替换、校验、发布和放弃草稿。
- 自动保存状态、修订号和最后更新时间可见。
- 摘要区显示记录、变更、错误、警告和相对正式版差异。
- 支持搜索、精确筛选、问题筛选、仅看修改和批量删除。
- 新增/编辑抽屉以两列 PC 表单组织字段。
- 发布弹窗必须在常见 PC 高度内保留底部操作。

### 超收规则

- 当前规则与发布表单分区清楚。
- 短尾、中尾、长尾输入显示“件”。
- 仓库为空时明确显示“未开放任何仓库”。
- 历史版本表完整展示版本、额度、仓库、时间和启用操作。

### 用户与操作记录

- 用户页显示账号总数、管理员、操作员和停用数量。
- 当前账号不可停用。
- 操作记录提供搜索、操作类型和操作人筛选。
- 操作记录当前范围为最近 200 条。

## 5. 视觉规范

- 主色：`#176b5b`。
- 页面背景使用浅中性灰，业务内容使用白色表面。
- 状态颜色仅表达就绪、警告、失败和待处理。
- 字体保持 `Inter, Microsoft YaHei, sans-serif`。
- 标题、说明、表格和辅助信息使用稳定层级，不堆叠大字号。
- 常规区块间距约 16–18px。
- 控件圆角、边框和阴影保持克制。
- 图标优先使用现有 Ant Design 图标。
- 不使用 emoji 或临时占位图片作为正式控件。

## 6. 交互安全

- 计算、发布、删除、放弃草稿和拆分均使用明确按钮文字。
- 请求执行期间禁用重复提交和离开操作。
- 服务器错误显示原操作失败原因，不伪装为成功。
- 库位草稿使用服务端修订号和基线版本保护。
- 历史规则不可编辑，只能发布新版本或重新启用。
- 页面刷新不得丢失已由服务器持久化的任务或草稿状态。

## 7. 可访问性基线

- 搜索框、筛选器、表格和图标按钮有可读名称。
- 表单字段使用可见标签。
- 状态不能只依赖颜色表达。
- 键盘焦点保持可见。
- 空状态说明当前无数据或无匹配结果。

当前浏览器验收只证明关键操作控件有名称和可见焦点，不等同于完整 WCAG 合规认证。

## 8. PC 验收范围

主要视口：

```text
1280 × 800
1440 × 900
1920 × 1080
```

每次主要界面调整至少检查：

- 页面无意外横向溢出。
- 标题、主操作和关键状态位于合理首屏范围。
- 表格操作列可见。
- 抽屉和弹窗底部操作可达。
- 切换页面后回到顶部。
- 登录过期能够返回登录页。
- 浏览器控制台无新增错误。

## 9. 自动化验证

前端：

```bash
cd frontend
npm run test
npm run build
```

涉及接口、业务状态或导出时还要运行：

```bash
source .venv/bin/activate
python -m unittest discover -s tests -v
python -m pip check
```

路由、下载和时间显示应在真实浏览器中复核。

## 10. 后续修改原则

- 根据真实操作反馈做小改动。
- 先定位现有测试，再修改交互。
- 不为消除构建体积提示优先引入复杂拆包。
- 不为少量内部用户增加移动端、主题系统或组件平台。
- 不通过 UI 变化改变采购、超收、仓库和拆分含义。
- 大范围视觉改版必须重新核对批次主流程和管理员维护流程。
