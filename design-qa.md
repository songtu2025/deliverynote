# DeliveryNote PC 界面验收摘要

- 验收范围：PC 浏览器
- 最后复核：2026-07-23
- 当前前端资源：`index-DzkyLbfg.js` / `index-BJrakn5L.css`
- 自动化基线：8 个测试文件，71/71 通过

本文记录当前可复核的界面结论，不再依赖会话临时目录或个人工具目录中的截图。

## 1. 持久证据

管理员维护的脱敏截图和浏览器指标保存在：

```text
design/admin-maintenance-qa/
```

内容包括：

- 1280×800、1440×900、1920×1080 的基础资料页面。
- 库位草稿工作区。
- 新增和编辑抽屉。
- Excel 替换差异。
- 发布错误和发布警告。
- 用户账号页面。
- 操作记录页面。
- `audit-evidence.json`。
- `capture-metrics.json`。

这些文件不包含真实密码、Token 或业务 Excel。

## 2. 批次工作台

最后验收结论：

- 批次详情使用全宽工作台，不受全局侧栏挤压。
- 当前阶段、数量守恒、来源顺序和锁定资料均可见。
- 待处理存在时，拆分审校是明确主操作。
- 搜索及六类筛选有可见标签和可读名称。
- 操作列在目标 PC 视口内可达。
- 拆分抽屉展示来源、定位、数量和守恒信息。
- 多文件合并下载和分文件 ZIP 位于同一结果操作区。
- 页面没有重复的底部导出卡片。

已验证交互：

- 打开批次详情。
- 点击主操作定位待处理区域。
- 使用原因筛选缩小结果。
- 打开并关闭拆分抽屉而不保存。
- 下载合并 Excel 和分文件 ZIP。
- 浏览器刷新保持当前批次。
- 浏览器返回回到批次列表。

## 3. 批次列表与超收规则

最后验收结论：

- 批次列表搜索和状态筛选有可见标签。
- 表格有明确可访问名称。
- 新建批次弹窗可正常打开和取消。
- 超收规则当前状态、发布表单和历史版本层次清楚。
- 空仓库白名单明确显示“未开放任何仓库”。
- 历史版本的重新启用操作包含版本名称。
- 页面切换会回到顶部。

当前维护不再把手机端作为完成标准。

## 4. 基础资料与库位维护

最后验收结论：

- 五类资料位于同一横向目录。
- 当前资料状态、版本、文件、创建人和更新时间在同一摘要中。
- 用途、必填字段和业务影响可以展开查看。
- 数据预览、版本记录和质量检查使用标签页。
- 库位已有正式版本时不提供普通替换上传入口。
- 库位维护顶部操作、自动保存状态和草稿摘要保持可见。
- 记录筛选、批量操作、抽屉和发布弹窗适合 PC 操作。
- 多条发布警告不会再把底部按钮推离目标视口。

持久截图覆盖了正常、错误、警告和抽屉状态。

## 5. 右上角账号区

最后验收结论：

- 显示头像首字母、用户名和中文角色。
- 退出操作是独立按钮，名称为“退出登录”。
- 键盘焦点可见。
- 退出会调用服务端登出、清除本地会话并回到登录页。
- 不增加下拉菜单、个人资料页或额外权限行为。

## 6. 北京时间与路由

真实 Chrome 曾使用 `America/Los_Angeles` 浏览器时区复核：

- API 返回显式 UTC `Z`。
- 页面仍按 `Asia/Shanghai` 显示业务时间。
- `/batches/{id}` 刷新保持同一批次。
- 浏览器前进和后退能够同步页面。
- 验收期间没有失败响应或控制台错误。

这部分结论由功能测试和生产浏览器复核共同支持，不依赖截图比对。

## 7. 自动化结果

最近完整结果：

```text
Frontend Vitest: 8 files, 71/71 passed
Frontend production build: passed
Python unittest: 131/131 passed
pip check: passed
Docker Compose config: passed
git diff --check: passed
```

生产构建仍有单包超过 500 kB 的提示，不是构建失败。

## 8. 当前已知限制

- 没有宣称完整 WCAG 合规。
- 不维护手机端专项布局。
- 批次列表、规则页和管理页的数据量仍按少量内部使用设计。
- 操作记录只显示最近 200 条。
- 早期批次页面的部分对比截图只存在于开发临时目录，未作为仓库证据保留。

## 9. 后续界面修改复核清单

1. 运行前端完整测试和生产构建。
2. 在 1280×800 和 1440×900 检查主要流程。
3. 检查页面和关键表格是否意外横向溢出。
4. 检查弹窗与抽屉底部操作是否可达。
5. 检查按钮、筛选器和表格的可读名称。
6. 复核登录过期、刷新、返回和下载。
7. 涉及时间时使用非北京时间浏览器复核。
8. 涉及业务数量时同时运行 Python 测试和脱敏业务场景。

当前 PC 界面可以继续作为少量内部用户的操作基线；后续应以真实使用反馈驱动，而不是继续做无业务依据的大范围视觉重构。
